Suggest the single next logical thing to build for FinFlow.

Consider:
1. What React or Redux concepts haven't been introduced yet that would be valuable to learn
2. What would make the dashboard more functionally complete (closer to a real Mercury-style UI)
3. Whether it's a natural progression from what was last built
4. Whether it's scoped small enough to build and document in one session

Concepts not yet introduced (track as these get built):
- useDispatch (writing to the store)
- useEffect (side effects and lifecycle)
- useState (local component state)
- React Router (navigation between pages)
- Controlled form inputs
- Custom hooks
- Async Redux (createAsyncThunk)
- Context API
- Memoization (useMemo, useCallback, React.memo)

---

Respond in this exact format:

## Next: [Name of thing to build]

**What it is**
One sentence description of the component, feature, or pattern.

**Why now**
Why does this come next? What does it build on from what's already done, and what new concept does it introduce?

**New concept introduced**
Name the primary React or Redux concept this would teach. Explain it in 2–3 sentences — plain English.

**Scope**
What files would be created or modified? Keep it to 1–3 files.

**When you're ready**
Say: "Type `/build [name]` or just ask me to build it and I'll start."

---

Suggest ONE thing only. Do not give a list of options.
